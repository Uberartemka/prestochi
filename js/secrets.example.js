/* Скопируй этот файл как secrets.js и подставь свой Mapbox public token.
   secrets.js игнорируется git-ом (.gitignore). */
window.PRESTOCHI_SECRETS = {
  mapboxToken: 'pk.YOUR_MAPBOX_PUBLIC_TOKEN_HERE',
};
